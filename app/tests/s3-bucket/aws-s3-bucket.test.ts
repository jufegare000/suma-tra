import { assert } from "chai";
import AWSConfig from '../../config/aws/aws';

describe('AWS S3 bucket testing', function () {

  
  it.only('create a folder for solicitante', async function () {

    AWSConfig
  });
});
